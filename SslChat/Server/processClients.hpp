
#ifndef PROCESSCLIENTS_HPP
#define PROCESSCLIENTS_HPP

// standard headers
#include <string>

int processClients(std::string    certPath,
                   std::string    keyPath,
                   unsigned short port);

#endif